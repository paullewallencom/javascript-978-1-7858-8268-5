{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src -d lib --source-maps"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.6.0"
  }
}
